# =============================================================================
# NINO WASH - Configuration Production
# =============================================================================
# ⚠️ IMPORTANT: DO NOT COMMIT ACTUAL SECRETS TO THIS FILE
# Copy to .env.production and fill with real production values

# -----------------------------------------------------------------------------
# APPLICATION
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=https://app.ninowash.com
NODE_ENV=production

# -----------------------------------------------------------------------------
# SUPABASE (Production Database)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://your-production-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-production-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-production-service-role-key
SUPABASE_JWT_SECRET=your-production-jwt-secret

# ⚠️ CRITICAL: This must be kept secret and configured in your deployment platform
# (Vercel secrets, GitHub secrets, or your hosting provider's env var system)

# -----------------------------------------------------------------------------
# STRIPE (Production)
# ⚠️ Switch from test keys (pk_test_/sk_test_) to live keys (pk_live_/sk_live_)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_your-production-publishable-key
STRIPE_SECRET_KEY=sk_live_your-production-secret-key

# ⚠️ CRITICAL: Obtain this from Stripe Dashboard → Developers → Webhooks
# After creating the webhook endpoint for https://app.ninowash.com/api/webhooks/stripe
STRIPE_WEBHOOK_SECRET=whsec_your-production-webhook-secret

# -----------------------------------------------------------------------------
# RATE LIMITING
# -----------------------------------------------------------------------------
RATE_LIMIT_REQUESTS_PER_MINUTE=60
RATE_LIMIT_WINDOW_MS=60000

# -----------------------------------------------------------------------------
# FEATURE FLAGS
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUBSCRIPTIONS_ENABLED=false
NEXT_PUBLIC_BOOKINGS_ENABLED=true
MAINTENANCE_MODE=false

# -----------------------------------------------------------------------------
# FILE UPLOAD
# -----------------------------------------------------------------------------
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------
LOG_LEVEL=info
ENABLE_REQUEST_LOGGING=true

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
# For Vercel/Netlify deployment:
# 1. Add these env vars in dashboard (using live Stripe keys, not test keys)
# 2. Never commit real .env.production to git
# 3. Use this file (.env.production.example) as a reference template
#
# For obtaining production values:
# - Supabase: Project Settings → API
# - Stripe: Dashboard → API Keys (switch toggle to "Live" mode)
# - Stripe Webhook: Dashboard → Developers → Webhooks → Add endpoint
#   - URL: https://app.ninowash.com/api/webhooks/stripe
#   - Events: customer.subscription.*, charge.updated, invoice.*
#   - Copy signing secret displayed after creating webhook
